<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Matches</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 p-8">
<div class="max-w-5xl mx-auto bg-white p-6 rounded-lg shadow-md">
    <h1 class="text-2xl font-bold mb-4">Matches</h1>
    <% if (matches.length > 0) { %>
        <table class="min-w-full border-collapse border border-gray-300">
            <thead class="bg-gray-200">
            <tr>
                <th class="border border-gray-300 px-4 py-2">Home Team</th>
                <th class="border border-gray-300 px-4 py-2">Away Team</th>
                <th class="border border-gray-300 px-4 py-2">Referee</th>
                <th class="border border-gray-300 px-4 py-2">Commentator</th>
                <th class="border border-gray-300 px-4 py-2">Stadium</th>
                <th class="border border-gray-300 px-4 py-2">Date</th>
            </tr>
            </thead>
            <tbody>
            <% matches.forEach(match => { %>
                <tr>
                    <td class="border border-gray-300 px-4 py-2"><%= match.homeTeam.name %></td>
                    <td class="border border-gray-300 px-4 py-2"><%= match.awayTeam.name %></td>
                    <td class="border border-gray-300 px-4 py-2"><%= match.referee.name %></td>
                    <td class="border border-gray-300 px-4 py-2"><%= match.commentator.name %></td>
                    <td class="border border-gray-300 px-4 py-2"><%= match.stadium.name %></td>
                    <td class="border border-gray-300 px-4 py-2"><%= new Date(match.date).toLocaleString() %></td>
                </tr>
            <% }); %>
            </tbody>
        </table>
    <% } else { %>
        <p class="text-gray-600">No matches available.</p>
    <% } %>
    <div class="mt-4">
        <a href="/addMatch" class="text-blue-600 hover:underline">Add a New Match</a>
    </div>
</div>
</body>
</html>
